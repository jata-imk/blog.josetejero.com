# Node / Next / Payload (aplican una vez exista blog/src)
node_modules/
.next/
dist/
build/
.turbo/

# Env y secretos
.env
.env.*
!.env.example

# Logs / OS / editor
*.log
.DS_Store
Thumbs.db

# Claude Code (config/estado local del harness)
.claude/

# Uploads locales de Payload (media generada)
blog/media/

# QA artifacts (testing screenshots, capturas y logs de Playwright)
# Cubre toda la raíz y blog/: las capturas de QA usan prefijo tod-<n>-... y *-rerun/-final.
.qa-artifacts/
artifacts/
.playwright-mcp/
test-results/
admin-login*.png
tod-*.png
*-rerun.png
*-final.png

# Evitar que las capturas temporales o JSONs en la raíz del repo queden sin ignorar
/*.png
/*.json

# TypeScript incremental build
*.tsbuildinfo
