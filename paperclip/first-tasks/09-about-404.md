# Tarea 09 — Sobre mí + 404

**Asignar a:** Frontend · **Depende de:** 03 (tokens), 03b (componentes) · **Tipo:** rutas
**Puede ir temprano / en paralelo:** solo usa tokens y componentes; no depende de datos ni de 04-08.

> Las dos pantallas más simples del handoff. Reutiliza shells existentes; **no re-maquetes**.

## Prompt para el issue
> **1. Sobre mí** (`app/(frontend)/sobre-mi/page.tsx`): compón la pantalla del handoff con
> `AuthorCard` y los chips `Skill` (agrupados por área), bio y links sociales. Contenido puede ser
> estático por ahora (o leerse de un global de Payload si ya existe; si no, no lo inventes —
> escala). Tokens, cero hardcodeo.
> **2. 404** (`app/(frontend)/not-found.tsx`): el código `404` a 150px con gradiente (`.code-404`/
> `--grad`), mensaje y botón de volver al inicio (`Btn variant="grad"`).

## Done cuando
- `/sobre-mi` renderiza fiel al handoff con componentes existentes.
- Una ruta inexistente muestra el 404 diseñado. `pnpm lint` pasa.
- agent-note breve (puede ser conjunta). QA: gate visual (desktop + móvil) de ambas.
